<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Cleartext blocked by default. -->
    <base-config cleartextTrafficPermitted="false" />
    <!-- Dev + LAN hosts only: emulator, localhost, and the operator LAN server.
         Self-hosted deployments should use HTTPS in production; these entries
         keep local/operator testing working in the release build. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">192.168.29.240</domain>
    </domain-config>
</network-security-config>
